{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.0",
  "author": "Daniel Beck <daniel@beck.win>",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "generate": "lerna run generate",
    "lerna-publish": "lerna publish --force-publish @daisy-chain/components,@daisy-chain/components-react,@daisy-chain/design-tokens"
  }
}
